import { createMuiTheme } from '@material-ui/core/styles'

const mixtoTheme = createMuiTheme({
    palette: {
        primary: {
            main: '#35e6cb'
        },
        secondary: {
          main: '#452984',
        }
      }
  });

export default mixtoTheme;